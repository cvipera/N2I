fishes = document.getElementsByName("fishes");

fishes.addEventListener( "change", ()=>{
    fishes.value= "+";
});

const button = document.getElementById('submit');

button.addEventListener('click', () => {})